int main(void){
    int i=42;
    prontf("La respuesta es %d\n", i);
}

/*
C) Si eliminamos o agregamos un parametro genera un error cuando intentamos compilar el programa,
ya que la invocacion no es compatible con la declaracion de la funcion. Este error se 
detecta en el analisis semantico del proceso de compilacion.
*/
